import React from "react";
import ReactDOM from "react-dom/client";
import ProjectorSelector from "./ProjectorSelector.tsx";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
    <>
        <ProjectorSelector/>
    </>
);
